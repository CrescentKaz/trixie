const person = document.getElementById(person);
const greeting = document.getElementById(greeting);
const question = document.getElementById(question);

const names = ["Mark", "Chris", "Katy", "Yashi", "Tasha", "Ning"];

// get time of day to determine the greeting
// show chunk1 for 2-3 sec before displaying chunk2 instead.
// idr what i was planning for chunk3...